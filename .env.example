# Famile agent — LLM providers (server-only; never NEXT_PUBLIC_*)
#
# Default failover order: Venice → 0G Router → Anthropic → recorded replay.
# Override with: LLM_PROVIDER_ORDER=venice,og,anthropic

# Venice AI (OpenAI-compatible)
# https://docs.venice.ai/
VENICE_API_KEY=
# VENICE_BASE_URL=https://api.venice.ai/api/v1
# VENICE_MODEL=zai-org-glm-5-1

# 0G Compute Router (OpenAI-compatible)
# https://docs.0g.ai/developer-hub/building-on-0g/compute-network/inference
# API key from https://pc.0g.ai (sk-…)
OG_API_KEY=
# OG_BASE_URL=https://router-api.0g.ai/v1
# OG_MODEL=zai-org/GLM-5-FP8

# Anthropic (optional fallback)
# LLM_API_KEY=
# ANTHROPIC_API_KEY=
# LLM_MODEL=claude-3-5-sonnet-latest

# LLM_PROVIDER_ORDER=venice,og,anthropic

# Base44 shared Mira memory store (optional). When set, /api/agent proxies to
# the Base44 miraAnswer function and /api/agent/history hydrates prior turns.
# When unset, the Ask surface falls back to the local engine (stateless).
#
# BASE44_APP_ID is server-only (route handlers). NEXT_PUBLIC_BASE44_APP_ID is
# client-visible (dashboard realtime polling, orb posture sync). Set both to
# the same value.
BASE44_APP_ID=
NEXT_PUBLIC_BASE44_APP_ID=

# Optional explicit voice transport. The browser gets a short-lived ticket
# from /api/voice/session, then connects to this gateway. When unset,
# supported browsers use native speech recognition as a local fallback.
# VOICE_GATEWAY_URL=wss://voice.example.com/v1/stream
# VOICE_GATEWAY_SECRET=replace-with-a-long-random-secret
# VOICE_ALLOWED_ORIGINS=https://famile.xyz,http://localhost:3000
# SPEECHMATICS_WS_URL=wss://your-speechmatics-endpoint/v2
# SPEECHMATICS_API_KEY=
# SPEECHMATICS_LANGUAGE=en
# NEXT_PUBLIC_VOICE_WS_URL=wss://voice.example.com/v1/stream
